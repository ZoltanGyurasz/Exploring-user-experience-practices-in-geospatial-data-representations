module.exports = {
    chainWebpack: config => {
        const geojsonRule = config.module.rule('geojson')

        // clear all existing loaders.
        // if you don't do this, the loader below will be appended to
        // existing loaders of the rule.
        svgRule.uses.clear()

        // add replacement loader(s)
        svgRule
            .use('vue-loader')
            .loader('vue-loader')
    }
}